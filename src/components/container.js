import React from 'react'
import { Helmet } from "react-helmet"
import $ from "jquery"


export default ({ children }) => (
  <div id="main">
  
  <div className="inner">
  {children}
  </div>
  
  
  </div>
)
